{
  "name": "dcda-advisor-functions",
  "description": "Cloud Functions for the DCDA Advising Wizard",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "node --check *.js",
    "test": "node --test",
    "serve": "firebase emulators:start --only functions,firestore,hosting",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.1",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^7.0.5"
  }
}
